namespace Protokite.Playtest
{
    /// <summary>This package's version, sent with each session. Bumped with package.json; CI checks the two match.</summary>
    internal static class ProtokitePlaytestVersion
    {
        internal const string Current = "1.47.0";
    }
}
